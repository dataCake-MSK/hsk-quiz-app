# 2026-09-24 일간 일지

## 요약
사용자 결정으로 **인증 작업을 보류**하고(PR #36 닫음), 현황·구축 계획 리포트를 정리했다(PR #37). 이어서 계획 승인을 받아 **이슈 #10(단어 테이블·샘플 시드·목록 API)** 을 구현해 PR #39를 올렸다.

## 지표
| 커밋 | 머지된 PR | 닫힌 이슈 | 열린 이슈 |
|---|---|---|---|
| 7 (+ merge 2) | 2 (#37, #38) | 0 | 16 |

## 주요 활동
- 사용자 질문에 답변: 회원가입은 **외부 서비스 없이 직접 구현**한 것(pwdlib + 우리 DB + 우리 엔드포인트)
- PR #36 닫고 브랜치 삭제 — 코드는 닫힌 PR에 남아 재개 시 복원 가능
- 이슈 #5~#8에 보류 사유 코멘트, `priority:high` → `priority:low`
- 문서 반영: NB-019(인증 보류), SRS-010~013 `deferred`, SRS-014 `done`, 보류 기간에는 공용 데이터만 다루도록 조정
- 리포트 [현황과 구축 계획](../reports/2026-09-24-status-and-plan.md) — 기능·API·DB 현황, 테이블 추가 순서, A~H 단계
- **이슈 #10 구현**: `words` 테이블(+마이그레이션 0002), 샘플 20단어 시드 CLI, `GET /words?hsk_level=`
  - 공용/개인 중복을 각각 막는 **부분 유니크 인덱스** 사용
  - 헷갈리는 쌍(买/卖, 他/她, 白/百, 左/右, 借/还, 简单/简历, 认识/知道)을 샘플에 일부러 포함 — #16의 혼동 관계 시드에서 바로 쓸 수 있게

## 완료한 이슈 / PR
- PR #36 [SRS-010] **닫힘**(보류)
- PR #37 문서 보류 반영 + 현황 리포트 머지 (🟢)
- PR #38 일지 머지 (🟢)
- PR #39 [SRS-020] 열림 — AC 6개 확인, CI 4종 초록, 🟡 사용자 머지 대기

## 결정 사항 (ADR)
- 새 ADR 없음. 사용자 결정 3건
  1. 인증 관련 작업 보류(NB-019)
  2. 단어 기능부터 진행(인증 없이 공용 데이터만)
  3. HSK 기준이 정해질 때까지 **개발용 샘플 20단어**로 진행(NB-016은 계속 hold)

## 막힌 점
- 시드 CLI 첫 실행이 `UnicodeEncodeError: 'cp949' codec can't encode character '—'`로 실패. 한국어 Windows 콘솔이 em dash를 못 찍는다 → 출력 스트림을 UTF-8로 재설정하고 메시지에서 특수문자를 뺐다. (DB 삽입은 이미 커밋된 뒤라 데이터는 정상)
- 그 수정을 모듈 상단에 두니 ruff `E402`(import 위치) — 함수로 감싸 `main()` 첫 줄에서 호출

## 배운 점
- (사용자 질문은 대화로 처리: 회원가입 직접 구현 여부, 단어 메타데이터 구성)

## 다음 계획
1. PR #39 머지
2. 이슈 #11 앱 단어 목록·상세 화면 — 폰에서 급수 탭으로 단어를 보는 첫 화면
3. 이후 #13(카테고리) → #16(혼동 관계·세션) → #17~#19(출제·채점·결과)

---

## 개발자 퀵 가이드
> 이날 결과로 이어진 진행사항만 압축.

### 핵심 진행사항
1. **보류를 문서로 남기기**: NB 기록 → SRS `deferred` → 추적표 → 이슈 코멘트·라벨. PR은 머지하지 않고 닫아 커밋을 보존
2. **테이블 추가 흐름**: 모델 작성 → 마이그레이션 파일 → `alembic upgrade head` → 테스트 → 시드 → API
3. **부분 유니크 인덱스**로 공용/개인 중복을 따로 막기 — 시드를 여러 번 돌려도 안전
4. **시드는 "이미 있는 건 건너뛰기"** 로 구현해 멱등성 확보

### 핵심 명령
```bash
cd backend
uv run --env-file .env alembic upgrade head
uv run --env-file .env python -m app.infrastructure.seed words   # 2번 돌려도 총 20개
uv run --env-file .env pytest -q                                  # 24 passed
curl "http://127.0.0.1:8000/words?hsk_level=1"
```

### 핵심 코드·설정
`app/infrastructure/db/models.py` — 공용/개인 중복을 각각 막는 부분 유니크 인덱스
```python
Index("uq_words_shared_hanzi", "hanzi", unique=True,
      postgresql_where=text("owner_id IS NULL")),
Index("uq_words_owner_hanzi", "owner_id", "hanzi", unique=True,
      postgresql_where=text("owner_id IS NOT NULL")),
```

`app/infrastructure/seed/__main__.py` — 이미 있는 한자는 건너뛴다
```python
existing = set(session.execute(select(Word.hanzi).where(Word.owner_id.is_(None))).scalars().all())
for row in SAMPLE_WORDS:
    if row["hanzi"] in existing:
        skipped += 1
        continue
    session.add(Word(owner_id=None, **row))
```

`app/interface/routers/words.py` — 인증 재개 지점을 코드에 표시
```python
# TODO(NB-019): 인증 재개 시 로그인 필수 + owner_id IS NULL OR owner_id = 나
statement = select(Word).where(Word.owner_id.is_(None))
```
