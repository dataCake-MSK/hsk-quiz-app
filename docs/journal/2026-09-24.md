# 2026-09-24 일간 일지

## 요약
사용자 결정으로 **인증 작업을 보류**했다. 완성돼 있던 회원가입 PR #36을 닫고, 요구사항 문서를 보류 상태로 맞춘 뒤, 현재 구현·DB 현황과 앞으로의 구축 계획을 리포트로 정리했다(PR #37 머지). 다음은 단어 기능(#10).

## 지표
| 커밋 | 머지된 PR | 닫힌 이슈 | 열린 이슈 |
|---|---|---|---|
| 1 (+ merge 1) | 1 (#37) | 0 | 16 |

## 주요 활동
- 사용자 질문에 답변: 회원가입은 **외부 서비스 없이 직접 구현**한 것(pwdlib + 우리 DB + 우리 엔드포인트)
- PR #36 닫고 브랜치 삭제 — 코드는 닫힌 PR에 남아 있어 재개 시 되살릴 수 있음
- 이슈 #5~#8에 보류 사유 코멘트, `priority:high` → `priority:low`
- 문서 반영: NB-019(인증 보류) 추가, SRS-010~013 `deferred`, SRS-014 `done`, 단어 API는 보류 기간 동안 공용 데이터만 다루도록 조정, SRS-022·024는 인증 선행으로 표시
- 리포트 [현황과 구축 계획](../reports/2026-09-24-status-and-plan.md) 작성 — 기능·API·DB 현황, 테이블 추가 순서(A~H 단계), 보류 기간 규칙

## 완료한 이슈 / PR
- PR #36 [SRS-010] **닫힘**(머지하지 않음, 보류)
- PR #37 문서 보류 반영 + 현황 리포트 머지 (🟢 자체 머지)

## 결정 사항 (ADR)
- 새 ADR 없음. 사용자 결정 2건
  1. 인증 관련 작업 보류(NB-019) — 직접 구현 / 외부 서비스(Clerk 등) 선택은 단어·퀴즈를 써본 뒤
  2. 다음 작업은 단어 기능부터, 인증 없이 공용 데이터만

## 막힌 점
- 없음

## 배운 점
- (사용자 질문은 대화로 처리: 회원가입을 직접 구현했는지 여부)

## 다음 계획
1. 이슈 #10 단어 테이블·시드·목록 API — HSK 기준(NB-016) 미정이면 개발용 샘플 20단어로 진행
2. 이슈 #11 앱 단어 목록·상세 화면
3. 이후 #13 → #16 → #17~#19 순서로 퀴즈 핵심까지

---

## 개발자 퀵 가이드
> 이날 결과로 이어진 진행사항만 압축.

### 핵심 진행사항
1. **보류 결정을 문서에 남기는 방법**: NB에 니즈로 기록 → SRS 상태를 `deferred` → 추적표 갱신 → 이슈에 코멘트·라벨. 나중에 "왜 멈췄는지"를 다시 찾을 수 있다
2. **작업을 버리지 않고 보류**: PR을 머지하지 않고 닫으면 커밋이 PR 기록에 남아 재개할 때 복원할 수 있다
3. **스키마는 유저별 분리를 전제로 유지**하고 값만 임시로 채우기 — 인증이 나중에 붙어도 테이블을 갈아엎지 않는다. 임시 자리에는 `TODO(NB-019)` 표시

### 핵심 명령
```bash
gh pr close 36 --delete-branch --comment "<보류 사유>"
gh issue comment 5 --body "<보류 사유>"
gh issue edit 5 --add-label priority:low --remove-label priority:high

# 문서 PR은 CI 통과만 확인하고 자체 머지
gh pr checks <n> --watch --interval 20
gh pr merge <n> --merge --delete-branch
```

### 핵심 코드·설정
SRS에 보류 규칙을 한 곳에 못박아 두면 이후 이슈마다 판단이 흔들리지 않는다.
```markdown
- **인증 보류 (2026-09-24, NB-019)**: SRS-010~013은 `deferred`.
  그 전까지 API는 **공용 데이터만** 다루고, 유저별 기능(즐겨찾기·숙련도·세션 소유)은 인증 이후로 미룬다.
```
