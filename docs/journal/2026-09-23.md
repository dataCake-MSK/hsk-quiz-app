# 2026-09-23 일간 일지

## 요약
PR #35(alembic 인코딩 수정)를 머지하고 이슈 #5(회원가입 API)를 구현했다. Argon2 해싱, 이메일 소문자 정규화, 중복 409, rate limit까지 붙여 PR #36을 올렸다.

## 지표
| 커밋 | 머지된 PR | 닫힌 이슈 | 열린 이슈 |
|---|---|---|---|
| 6 (merge commit 1 포함) | 1 (#35) | 0 | 17 |

## 주요 활동
- PR #35 머지, 브랜치 정리
- `POST /auth/register` 구현
  - `pwdlib[argon2]`로 해싱하고 모듈(`infrastructure/security/password.py`)로 감쌈 — 알고리즘이 바뀌어도 호출부는 그대로
  - 이메일은 스키마에서 소문자로 정규화(DB에도 CHECK가 있어 이중 방어)
  - UNIQUE 위반을 409로 변환, 응답에는 해시를 넣지 않음
  - SRS-014에서 준비한 `AUTH_RATE_LIMIT`을 이 라우터에 적용
- 테스트 10개 추가(전체 26개 통과), 실제 서버와 psql로도 확인

## 완료한 이슈 / PR
- PR #35 머지
- PR #36 [SRS-010] 열림 — AC 6개 모두 확인, 🔴 사용자 머지 대기

## 결정 사항 (ADR)
- 새 ADR 없음. Repository 패턴은 아직 도입하지 않음(ADR-0003의 도입 시점은 SRS-025) — 라우터에서 세션을 직접 사용

## 막힌 점
- curl로 한글이 든 JSON을 보내면 `There was an error parsing the body`(400). Git Bash가 인자의 UTF-8을 변환해 본문이 깨진 것 → **JSON을 UTF-8 파일로 쓰고 `--data-binary @파일`로 전송**해 해결

## 배운 점
- (없음 — 사용자 질문은 대화로 처리)

## 다음 계획
1. PR #36 머지
2. 이슈 #6 로그인 API와 `/users/me` — 여기서도 `AUTH_RATE_LIMIT` 적용, JWT는 `JWT_SECRET` 없으면 서버 시작 실패

---

## 개발자 퀵 가이드
> 이날 결과로 이어진 진행사항만 압축.

### 핵심 진행사항
1. **해싱은 모듈로 감싸기**: `hash_password` / `verify_password`만 노출해 알고리즘 교체 지점을 한 곳으로
2. **정규화는 입구에서**: Pydantic `field_validator`로 이메일을 소문자로 만들고, DB CHECK로 한 번 더 막음
3. **중복은 DB에 맡기기**: 미리 SELECT로 확인하지 않고 INSERT 후 `IntegrityError`를 409로 변환(경쟁 조건에도 안전)
4. **rate limit 재사용**: 이전 이슈에서 만든 설정값을 라우터에 붙이기만 함

### 핵심 명령
```bash
cd backend
uv add "pwdlib[argon2]"
uv run --env-file .env pytest -q                       # 26 passed
uv run --env-file .env fastapi dev app/main.py

# 한글이 포함된 JSON은 파일로 (Git Bash에서 인자로 주면 깨짐)
node -e "require('fs').writeFileSync('reg.json', JSON.stringify({email:'a@example.com',password:'hsk-quiz-1234',nickname:'민트'}),'utf8')"
curl -X POST http://127.0.0.1:8000/auth/register -H 'Content-Type: application/json' --data-binary @reg.json
```

### 핵심 코드·설정
`app/interface/routers/auth.py` — 중복 검사를 DB 제약에 맡긴다
```python
with session_factory() as session:
    session.add(user)
    try:
        session.commit()
    except IntegrityError:
        session.rollback()
        raise HTTPException(status_code=409, detail="이미 가입된 이메일입니다.") from None
    session.refresh(user)
```

`app/interface/schemas/auth.py` — 입구에서 정규화
```python
@field_validator("email")
@classmethod
def normalize_email(cls, value: str) -> str:
    return value.strip().lower()
```
