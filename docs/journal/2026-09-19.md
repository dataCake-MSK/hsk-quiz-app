# 2026-09-19 일간 일지

## 요약
사용자가 PR #24(SRS-001)를 머지해 이슈 #1이 닫혔다. 사용자 요청으로 "PR 링크 아래 요약 보고" 규칙을 PR #25로 올렸고, 추적 문서를 머지 결과에 맞췄다.

## 지표
| 커밋 | 머지된 PR | 닫힌 이슈 | 열린 이슈 |
|---|---|---|---|
| 4 (merge commit 1 포함) | 1 (#24) | 1 (#1) | 20 |

## 주요 활동
- PR #24 [SRS-001] 머지 반영: SRS·추적표 상태 `done`, 로컬 브랜치 정리
- PR #25: CLAUDE.md·commit 스킬에 "PR 링크 아래 요약(무엇을·왜 / 주요 변경 / 검증 / 확인할 점 / 머지 등급)" 규칙 추가 (🔴, 사용자 머지 대기)
- 규칙 변경(🔴)과 문서 동기화(🟢)는 등급이 달라 PR을 나눔
- 사용자 질문(프론트·백엔드를 분리하나?) 답변을 학습 노트로 정리

## 완료한 이슈 / PR
- #1 [SRS-001] 백엔드 스캐폴딩 — PR #24 머지

## 결정 사항 (ADR)
- 새 ADR 없음. 모노레포 구조는 ADR-0001 그대로 유지

## 막힌 점
- 없음

## 배운 점
- [프론트엔드·백엔드 모노레포 vs 분리](../learning/monorepo-frontend-backend.md) — 저장소는 하나, 프로그램은 분리(HTTP API로만 통신)

## 다음 계획
1. PR #25 사용자 머지
2. 이슈 #2 SRS-002 Expo 스캐폴딩 + 폰에서 백엔드 접속 방식 확인

---

## 개발자 퀵 가이드
> 이날 결과로 이어진 진행사항만 압축.

### 핵심 진행사항
1. **머지 후 정리**: main 갱신 → 원격에서 지워진 브랜치 정리 → 로컬 브랜치 삭제 → 추적표 상태 `done`
2. **등급이 다른 변경은 PR 분리**: 규칙(🔴)은 사용자 머지, 문서 동기화(🟢)는 AI 자체 머지

### 핵심 명령
```bash
git switch main && git pull && git fetch --prune
git branch -d <머지된 브랜치>
gh issue view 1 --json state,closedAt      # Closes #1 로 자동으로 닫혔는지 확인
git stash push <파일...>                   # 다른 PR로 보낼 변경을 잠시 치워두기
git stash pop
```
